type Holiday {
  id: ID!
  date: DateTime!
  name: String!
}

extend type Query {
  holidays: [Holiday!]!
}

extend type Mutation {
  addHoliday(date: DateTime!, name: String!): Holiday!
  updateHoliday(id: ID!, date: DateTime, name: String): Holiday!
  removeHoliday(id: ID!): Boolean!
}
